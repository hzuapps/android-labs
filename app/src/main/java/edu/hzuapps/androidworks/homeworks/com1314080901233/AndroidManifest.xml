<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.intelligent_restranant_boss"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <!-- 添加操作振动器的权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 添加操作闪光灯的权限 -->
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <!-- 这个权限用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 这个权限用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 访问网络，网络定位,获取菜谱需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SD卡读取权限，用户写入离线定位数据 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!--保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        xmlns:tools="http://schemas.android.com/tools"
        tools:replace="android:icon"
        android:allowBackup="true"
        android:icon="@mipmap/myboss"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme.NoActionBar" >
        <activity
            android:name="com.example.intelligent_restranant_boss.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GuideActivity" />
        <activity android:name=".FrameActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MyPaymentActivity" />
        <activity android:name=".MyInfoActivity" />
        <activity android:name=".MyMoodActivity" />
        <activity android:name=".TodolistShowActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".MyActivity" />
        <activity android:name=".MoreActivity" />
        <activity android:name=".TodolistEditActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".CommonListActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".toDo.ToDo_Activity"
            android:theme="@style/TodoTheme"/>
        <activity android:name=".excel.ExcelActivity" />
        <activity
            android:name=".menu.MenuListActivity"
            android:configChanges="keyboardHidden|orientation"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity
            android:name=".menu.MenuAPIActivity"
            android:configChanges="keyboardHidden|orientation"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity
            android:name=".budget.MyBudgetActivity"
            android:configChanges="keyboardHidden|orientation"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity
            android:name=".budget.BudgetDetailActivity"
            android:configChanges="keyboardHidden|orientation"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <!-- Setting界面下 Start -->
        <activity
            android:name=".AboutUsActivity"
            android:label="@string/title_activity_more" />
        <activity
            android:name=".FeedbackActivity"
            android:label="@string/title_activity_more" />
        <activity
            android:name=".LoginDialog"
            android:label="@string/title_activity_more"
            android:theme="@style/MyDialog" />
        <activity android:name=".AccountAlterInfoActivity" />
        <activity android:name=".AccountAlterPwdActivity"
            android:theme="@style/MyDialog"/>

        <!-- Setting界面下 End -->
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:process=":remote" >
        </receiver>

        <!-- 天气预报需要的 -->
              <activity android:name=".weather.WeatherActivity" />
        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote" />
        <service android:name=".weather.WidgetService" />
        <service android:name=".weather.WidgetService2" />

        <receiver android:name=".weather.MyWeigetProviderInfo" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather" />
        </receiver>


        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="vh9OGRfjxHojRj5agS9sgFfD" />
    </application>

</manifest>